<section>
  <h1> Let's add menu </h1>

  <%= form_with url: menus_path do |form| %>

    <div>
      Menu Name  :<br/>
      <%= form.text_field :name %>
    </div>

    <%= form.submit "Create new menu !" %>
  <% end %>
</section>
